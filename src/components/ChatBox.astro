---
let date = new Date();
let time = () => {
    const minutes = date.getMinutes();
        if (minutes <= 9 ) {
            return `${date.getHours()}:0${date.getMinutes()}`
        }
    return `${date.getHours()}:${date.getMinutes()}`
}
---

<div class="flex flex-col w-full max-w-[420px] leading-1.5 p-4 border-gray-200 bg-gray-800 rounded-e-xl rounded-es-xl dark:bg-gray-700">
    <div class="flex items-center space-x-2 rtl:space-x-reverse">
       <h1 class="text-sm md:text-xl font-semibold text-white">David Guzman</h1>
       <span class="text-sm font-normal text-gray-500 dark:text-gray-400 pt-1">{time}</span>
    </div>
    <p class="text-sm md:text-lg font-normal py-2.5 text-white">I'm <strong class="text-cyan-600">Front-end developer</strong> with over 4 years of experience as a programmer. Specializing in the development of unique <strong class="text-cyan-600">web applications.</strong></p>
    <span class="text-sm font-normal text-gray-500 dark:text-gray-400 flex justify-end">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round">
        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
        <path d="M7 12l5 5l10 -10" />
        <path d="M2 12l5 5m5 -5l5 -5" />
        </svg>
    </span>
</div>